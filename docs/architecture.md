flowchart LR
  A[Scenario JSON Profiles\n data/scenarios/*.json] --> B[Scenario Parser\n Scenario.from_dict]
  B --> C[Risk Scoring Engine\n score_scenario(cfg)]
  C --> D[Explainability Trace\n reasons per category]
  C --> E[Normalization\n normalize_scores(max_scores)]
  E --> F[Prioritization\n ranked categories Top 3]
  F --> G[Recommendation Generator\n Phase 1/2/3 actions]
  G --> H[Standards Mapper\n NIST CSF + CIS Controls]
  H --> I[Outputs\n CLI report or JSON report]
  I --> J[Evaluation Layer\n baseline + sensitivity + practitioner review]
